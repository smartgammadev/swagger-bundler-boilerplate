schemas:
  $ref: schemas/index.yaml

parameters:
  $ref: parameters/index.yaml

headers:
  $ref: headers/index.yaml

responses:
  $ref: responses/index.yaml

securitySchemes:
   bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Use for Troops API
